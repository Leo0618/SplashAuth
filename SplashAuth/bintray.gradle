apply plugin: 'com.novoda.bintray-release'
publish {
    userOrg = 'bytehit'
    groupId = 'com.leo618'
    artifactId = 'SplashAuth'
    publishVersion = '0.0.3'
    desc = 'SplashAuth'
    website = 'https://github.com/Leo0618/SplashAuth'
}
tasks.withType(Javadoc) {
    options {
        encoding "UTF-8"
        charSet 'UTF-8'
    }
    options.addStringOption('Xdoclint:none', '-quiet')
}